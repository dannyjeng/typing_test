import React, { Component } from 'react';

const genText = () => {
    // placeholder
    const text = ['Sample', 'text', 'testing', 'what', 'am', 'I', 'doing?']
    return text
}

export default genText;
